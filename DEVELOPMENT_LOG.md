# DSH 屏幕助手插件 — 开发日志

> 本文件由 AI 在每轮工作后更新，记录做了什么、为什么、改了哪些文件、测了什么、还剩什么。
> 你随时可以打开这个文件看进度。最新内容在最上面。

---

## 2026-09-26 上午 III（第 7 轮工作）✅ 已完成

### 主题：前台保护自动接线 —— 「窗口还会到前台」彻底解决

| 改动 | 说明 |
|------|------|
| ui.click / keyboard.write 自动保护 | 每次后台投递后自动检测前台是否被目标应用抢走，被抢则自动执行 foreground restore（ALT trick 新进程夺回） |
| 结果字段 | foregroundRestored（true/false）+ originalForeground（原前台窗口句柄） |

### 实测（两条路径都验证）

1. ui.click 点设置「设置」控件 → foregroundRestored=true ✅
2. keyboard.write 写「前台保护」到搜索框 → setvalue 抢前台 → 自动夺回 foregroundRestored=true ✅
3. 测试后搜索框清空、页面恢复 ✅

### 发布

- 45/45 测试 ✅
- **v0.1.43 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.43
- 装机副本已同步 ✅

### 「同款的所有东西」完成度

- ✅ 视觉七件套（滑动光标/驻留/涟漪/打字脉冲/状态徽标/滚动箭头/任务面板）
- ✅ 任务面板自动接线
- ✅ 前台保护自动接线（本轮）
- ⬜ 可选：操作摘要卡片、设置页里的滚动接线

---
## 2026-09-26 上午 II（第 6 轮工作）✅ 已完成

### 主题：面板自动接线 —— 零配置的 Codex 体验

| 改动 | 说明 |
|------|------|
| 面板自动出现 | 每个后台操作（ui.click/mouse.click/keyboard.write/mouse.scroll）自动驱动面板：首个操作打开面板，每步自动更新描述与进度 |
| 自动收尾 | 6 秒无新操作，面板自动关闭（finish） |
| 步骤文案 | 自动生成中文描述：「点击「返回」」「输入「面板联动」」「滚动 2」 |

### 实测

- 连续两个操作（click + write）→ 面板自动出现并推进 2 步 → 空闲后自动关闭 ✅
- 45/45 测试 ✅
- **v0.1.42 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.42
- 装机副本已同步 ✅，设置搜索框已清空恢复

### 「完全仿照 Codex」总进度

- ✅ 视觉六件套 + 任务面板 + 自动接线（**全部完成**）
- ⬜ 可选增强：操作摘要卡片、滚动可视化插件接线

---
## 2026-09-26 上午（第 5 轮工作）✅ 已完成

### 主题：Codex 风格任务面板 —— 「同款的所有东西」核心件

| 新增 | 说明 |
|------|------|
| dsbox panel start | 启动常驻任务面板（独立进程、置顶、顶部居中）：任务标题 + 当前步骤（带活动圆点）+ 完成计数 + 进度条 |
| panel update | 推进步骤：--step 文案 --done N --total M，面板 300ms 轮询状态文件实时刷新 |
| panel stop | --finish 后面板 2.5 秒自动关闭 |

### 实测

- 面板全流程：start → 3 次 update（步骤推进 + 进度条增长）→ finish 自动关闭 ✅
- 45/45 测试 ✅
- **v0.1.41 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.41
- 装机副本已同步 ✅（lib/scripts 现含 status-panel.ps1）

### 「同款的所有东西」进度

- ✅ 视觉六件套（滑动光标/驻留/涟漪/打字脉冲/状态徽标/滚动箭头）
- ✅ 任务面板（本轮）
- ⬜ 插件侧自动接线（面板 start/update/stop 由插件在多步操作中自动调用）
- ⬜ 操作摘要卡片（完成后弹出"本次做了什么"）

---
## 2026-09-26 凌晨 III（第 4 轮工作）✅ 已完成

### 主题：状态徽标 + 滚动可视化 —— 「完全仿照 Codex」六件套集齐

| 新增 | 说明 |
|------|------|
| 状态徽标 | 屏幕顶部居中深色胶囊，实时显示 AI 在做什么：「AI 正在点击「返回」」/「AI 正在输入（N 字符）」，900ms 渐隐 |
| 滚动可视化 | scrolluia 滚动时在滚动区中心脉冲方向箭头（两次），滚动落地后消失 |

### 实测（设置应用全流程）

1. invoke「蓝牙和其他设备」：徽标「AI 正在点击…」+ 光标滑入 + SelectionItem 执行 ✅
2. scrolluia 50%：滚动箭头脉冲 + 滚动 0%→50% ✅
3. setvalue "codex"：徽标「AI 正在输入」+ 打字脉冲 + 写入 ✅
4. 全部恢复：搜索框清空、滚动回 0%、ESC 回主页 ✅

### 发布

- 45/45 测试 ✅
- **v0.1.40 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.40
- 装机副本已同步 ✅

### 「完全仿照 Codex」六件套 ✅ 全部完成

滑动光标 / 常驻驻留 / 点击涟漪 / 打字脉冲 / 状态徽标 / 滚动箭头

---
## 2026-09-26 凌晨 II（第 3 轮工作）✅ 已完成

### 主题：打字脉冲可视化 —— 「完全仿照 Codex」第二步

| 升级 | 说明 |
|------|------|
| 按键脉冲 | setvalue 写入时，输入框上方逐字符弹出深色徽标（显示正在写入的字符，最多 12 个、每个 45ms），像有人在打字 |
| 完整时序 | 元素框亮 → AI 光标滑入 → 字符脉冲逐个出现 → 写入完成 |

### 实测

- 设置搜索框写入 "hello 世界 123"：写入成功、读回验证 ✅、随后清空恢复、页面回到主页
- 45/45 测试 ✅
- **v0.1.39 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.39
- 装机副本已同步 ✅

### 「完全仿照 Codex」进度

- ✅ 光标滑动动画（0.1.37）
- ✅ 常驻光标 + 连续滑动（0.1.38）
- ✅ 点击涟漪（0.1.37）
- ✅ 打字脉冲（本轮）
- ⬜ 滚动可视化（下轮）
- ⬜ 状态徽标（AI 正在操作 xxx 小浮标）

---
## 2026-09-26 凌晨（第 2 轮工作）✅ 已完成

### 主题：「完全仿照 Codex」第一步 —— 常驻 AI 光标 + 连续滑动

| 升级 | 说明 |
|------|------|
| 光标常驻 | AI 光标操作后**驻留在原地**不消失（dsbox 退出时才销毁），下次操作从那里继续 |
| 跨调用连续滑动 | 位置存 %TEMP%\dsbox-cursor.json，每次操作从上次位置滑向新目标 —— 连续操作读起来像一段连续的光标旅程 |

### 实测

- 设置应用三连导航：蓝牙和其他设备 → 系统 → 个性化，光标连续滑过三个位置 ✅
- 45/45 测试 ✅
- **v0.1.38 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.38
- 装机副本已同步 ✅

---
## 2026-09-25 深夜 VIII（第 35 轮工作）✅ 已完成

### 主题：Codex 风格动画光标 —— 你要的"灵活一点"

| 升级 | 说明 |
|------|------|
| 滑动动画 | AI 光标从上一个操作位置平滑滑到新目标（16 帧、280ms、ease-out 缓动），不再瞬移 |
| 点击涟漪 | 到达目标后扩散一圈橙色涟漪（5 帧动画），示意"点了这里" |
| 渐隐退场 | 操作完成后光标渐隐 |

### 实测

- 动画函数单元测试通过（3 连滑动演示）
- 真实操作：设置「返回」按钮点击，光标滑过去 + 涟漪 + 点击 ✅
- 45/45 测试 ✅
- **v0.1.37 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.37
- 装机副本已同步 ✅

---
## 2026-09-25 深夜 VII（第 34 轮工作）✅ 已完成

### 主题：「你的光标呢」—— AI 自己的光标上线

你问 AI 操作时自己的光标在哪。答案：UIA 通道操作从不需要移动物理光标，所以之前确实"没有光标可看"。

**本轮新增：AI 的橙色箭头指针** —— 每次操作时，在操作点（按钮中心/输入框中心）显示一个半透明橙色 AI 箭头，停留 700ms 后渐隐：

| 特性 | 说明 |
|------|------|
| 颜色 | 橙色（你的光标是白色，一眼区分） |
| 行为 | 点击穿透、不抢焦点、不移动你的物理光标 |
| 覆盖 | ui invoke / keyboard setvalue / mouse click 三条主力路径 |
| 时序 | 先亮元素框 → 再出现 AI 箭头 → 操作执行 → 双双渐隐 |

### 实测

- 设置窗口点「返回」按钮：ok=true, frame=true, cursorMoved=false ✅
- 45/45 测试 ✅
- **v0.1.36 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.36
- 装机副本已同步 ✅

---
## 2026-09-25 深夜 VI（第 33 轮工作）✅ 已完成

### 主题：排查「没有光标」反馈

**结论：系统光标完全正常**（GetCursorInfo：flags=1 可见、句柄有效、位置实时更新）。

你看到的现象其实是**零接触设计本身**：UIA 通道操作（Invoke 点击/SetValue 打字）是应用"自己执行"动作，光标物理上从不需要移过去 —— 所以你不会看到鼠标出现在被点的按钮上。这正是"不抢鼠标"承诺的表现，视觉提示由 0.1.34 加的**元素级框**承担（点哪个按钮就亮哪个框）。

**如果你的意思是**：希望操作时光标能移动到目标上（更有"真实操作感"）→ 那需要 real 模式（会动你的真实光标）。告诉我你的偏好，我可以加一个 config 开关。

**本轮顺手加固**：foreground restore 的 ALT 技巧现在会释放左右两个 ALT 变体 + 发一个零距离鼠标事件，确保任何键状态都不会残留（防御性改进，排查过程中顺便做的）。

### 发布

- 45/45 测试 ✅
- **v0.1.35 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.35
- 装机副本已同步 ✅

---
## 2026-09-25 深夜 V（第 32 轮工作）✅ 已完成

### 主题：修复「使用时没有边框」—— 你反馈的问题

**根因找到了**：0.1.27/0.1.29 加的 UIA 快路径（ui invoke 点击、setvalue 打字）绕过了 dsbox 的 click/write 命令，而框提示原本只挂在那两个命令里 —— 结果越是常用的路径越没有框。

**修复**：invoke 和 setvalue 现在都会先框住**目标元素本身**（比框整个窗口更精准 —— 点哪个按钮就亮哪个按钮，打字就亮输入框），900ms 渐隐。

### 实测

- 设置窗口：ui invoke 点「返回」按钮 → 元素框闪现 → 页面真的回到系统主页 ✅
- 45/45 测试 ✅
- **v0.1.34 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.34
- 装机副本已同步 ✅

---
## 2026-09-25 深夜 IV（第 31 轮工作）✅ 已完成

### 主题：前台保护 —— 回答你问的「为什么窗口会提到前台」

**原因**：读屏（OCR/树）从不碰前台；但 Invoke 点击/SetValue 写入会让目标应用"自己执行"动作，应用处理输入时会**自己把自己激活到前台**（UWP/Chromium 都这样）—— 焦点是被目标应用抢的，不是 dsbox 动的。

**修复**（实测多轮，过程曲折）：

| 尝试 | 结果 |
|------|------|
| dsbox 内 AttachThreadInput+SetForeground | ❌ 输给 UWP 异步激活（应用抢在前台保护跑完之后） |
| SwitchToThisWindow | ❌ 竞态同样输 |
| **新进程执行 ALT trick + AttachThreadInput** | ✅ **成功**（新增 oreground restore --hwnd 命令） |

关键发现：在**独立的新进程**里跑 ALT 按键模拟 + AttachThreadInput，SetForegroundWindow 就能成功（ALT 事件让系统认为该进程在处理用户输入，从而获得前台授予资格）。在 dsbox 原进程里做则必然输给 UWP 的异步激活。

### 实测闭环

`
前台=DSH → setvalue(设置) → 前台被抢到设置 ✅（复现问题）
→ dsbox foreground restore --hwnd 262540 → restored=true ✅
→ 前台回到 DSH ✅
`

### 发布

- 45/45 测试 ✅
- **v0.1.33 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.33
- 装机副本已同步 ✅
- 插件侧自动调用接线（下一轮收尾）

---
## 2026-09-25 深夜 III（第 30 轮工作）✅ 已完成

### 主题：ui tree —— agent 能"看懂"界面了

| 改动 | 说明 |
|------|------|
| dsbox 新增 ui tree | 导出窗口的无障碍子树：每个元素带 depth/name/role/能力标记（Invoke=可点、Value=可写、Scroll=可滚等）/坐标 |
| 修复虚元素崩溃 | 屏幕外/虚拟化元素的坐标是无穷大（∞），转 Int32 时溢出崩溃 —— 已钳制为 0 |

### 实测（设置窗口，深度 4）

`
[Window] 设置
  [Window] 设置 [Value]
    [MenuBar] 系统
    [Button] 最小化 [Invoke] / [Button] 最大化 [Invoke] / [Button] 关闭 [Invoke]
    [Edit] 搜索框 [Value]
    [Button] 返回 [Invoke]
`

agent 看一眼树就知道：哪些按钮能点（Invoke）、搜索框能写（Value）—— 配合 ui find（定位）+ ui invoke（操作）+ ui inspect（验证），完整的 UIA 工作流闭环：**看懂 → 定位 → 操作 → 验证**。

### 发布

- 45/45 测试 ✅
- **v0.1.32 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.32
- 装机副本已同步 ✅

### 下一步候选

- 插件接入 ui tree（新增 screen_automation 的读动作或作为 ui.click 的辅助）
- 真实场景批量回归
- README 卖点更新（零接触四通道 + 树导出）

---
## 2026-09-25 深夜 II（第 29 轮工作）✅ 已完成

### 主题：mouse.scroll 自动走 UIA（插件侧接线）

| 改动 | 说明 |
|------|------|
| mouse.scroll 后台模式升级 | 带 --hwnd/--title 时自动先走 scrolluia（应用自己滚），无可滚动元素才回落 WM_MOUSEWHEEL |
| --point 不再必需 | UIA 滚动不需要坐标（应用自己滚自己）；消息路径才需要 |
| 代码整理 | dsbox 的 spawn/JSON 解析抽成 spawnDsboxJson 共用函数（滚动快路径要复用） |

### 实测

- mouse.scroll --amount 2 --hwnd <设置> → 自动走 scrolluia → 滚动 0%→20%（2 格×10%）→ 1.3 秒 → 光标没动 ✅
- 45/45 单元测试 ✅
- **v0.1.31 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.31
- 装机副本已同步 ✅，测试后滚动位置已恢复

### 意义

到现在为止，**点（Invoke）、打（Value）、滚（Scroll）、读（OCR/UIA）四类操作全部自动走零接触通道** —— 用户的光标和键盘只在所有 UIA/消息路径都失败时才会被 real 模式触碰（且提前亮窗口框提示）。

### 下一步候选

- dsbox ui tree（子树导出，agent 理解界面结构）
- 真实场景批量回归
- 考虑把"零接触三件套"写进 README 的卖点区

---
## 2026-09-25 深夜（第 28 轮工作）✅ 已完成

### 主题：UIA 滚动（ScrollPattern）—— 零接触三件套集齐

| 改动 | 说明 |
|------|------|
| dsbox 新增 `mouse scrolluia` | 应用通过无障碍通道自己滚动自己：不发窗口消息、不动光标，WM_MOUSEWHEEL 被忽略的 UWP 应用也能滚 |
| 支持绝对/相对 | `--percent 50` 直接跳到 50%；`--amount -3` 相对滚动（每步=滚动区的 1/10） |
| 自动选目标 | 在目标窗口里自动找「可滚动范围最大」的面板，不用指定具体元素 |

### 实测

- 设置应用：`scrolluia --percent 50` → 滚动位置 0%→50% ✅；`--percent 0` 恢复 ✅；全程光标没动
- 45/45 单元测试 ✅
- **v0.1.30 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.30
- 装机副本已同步 ✅

### 里程碑：「不碰用户」零接触三件套

| 操作 | 通道 | 版本 | 碰鼠标 | 碰键盘 |
|------|------|------|--------|--------|
| 点按钮 | UIA InvokePattern | 0.1.27 | ❌ | ❌ |
| 打字 | UIA ValuePattern | 0.1.29 | ❌ | ❌ |
| 滚动 | UIA ScrollPattern | **本轮** | ❌ | ❌ |
| 读屏 | WinRT OCR / UIA 树 | 0.1.23/25 | ❌ | ❌ |
| 兜底 | real 模式（先亮框） | — | ✅ | ✅ |

### 下一步候选

- UIA 滚动的插件侧接入（mouse.scroll 后台模式自动优先 scrolluia）
- dsbox `ui tree`（子树导出）
- 大规模回归：真实场景批量测试

---

## 2026-09-25 晚（第 27 轮工作）✅ 已完成

### 主题：keyboard.write 自动走 ValuePattern（打字零键盘）

| 改动 | 说明 |
|------|------|
| 插件 `keyboard.write` 后台模式升级 | dsbox 在场 + 显式目标时，自动改走 UIA ValuePattern 写入（不打键盘、不动光标、不抢焦点）；没有可写 Edit 控件时自动回落 WM_CHAR |
| 实测 | 设置搜索框写入 `test123` → 读回验证 → 清空恢复，端到端 **1.3 秒** |

### 测试与发布

- 45/45 单元测试 ✅
- **v0.1.29 已发布**：https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.29
- 装机副本已同步 ✅
- 测试后已把设置应用搜索框恢复原状（清空 + ESC 回主页）

### 里程碑：「不碰用户」承诺基本闭环

| 操作 | 通道 | 碰鼠标 | 碰键盘 |
|------|------|--------|--------|
| 点按钮 | InvokePattern (0.1.27) | ❌ | ❌ |
| 打字 | ValuePattern (本轮) | ❌ | ❌ |
| 点坐标/滚动 | 后台消息 (0.1.24) | ❌ | ❌ |
| 全都不行的兜底 | real 模式 | ✅（带框提示） | ✅ |

### 下一步候选

- UIA ScrollPattern 滚动（连后台消息都不用）
- dsbox `ui tree`（子树导出，方便 agent 理解界面结构）
- Edge/Chrome 网页内容的 ValuePattern 支持验证

---

## 2026-09-25 下午（第 25 轮工作）✅ 已完成

### 你刚提的要求与决定

1. **你要"AI 自己的鼠标"**（不动你的光标）→ 找到了正解：**UIA InvokePattern**
   - 原理：让应用"自己按下自己"的按钮（走应用的无障碍通道），不模拟鼠标、不发窗口消息、光标纹丝不动
   - 已在计算器上实测成功：连点「加→五→等于」，显示屏变成 10，你的鼠标全程没动
2. **你要详细日志** → 就是这份文件，以后每轮工作都更新

### 本轮改动明细

| 文件 | 改了什么 | 为什么 |
|------|---------|--------|
| `G:\Temp\dsbox\dsbox.ps1` | 新增 `ui invoke --name N` 命令 | 核心新功能：元素级点击 |
| 同上 | 修复 `ui find` 角色名正则（输出不再带 `ControlType.` 前缀） | 输出更干净 |
| 同上 | 强制 stdout 用 UTF-8 + 文件加 BOM | 修中文乱码（Node 读 GBK 输出会变问号） |
| `src/index.ts`（插件） | `ui.click` 后台模式新增 fast path：定位到 UIA 元素后自动改走 `ui invoke` | 让插件自动享受"零光标点击" |

### 最终测试结果（全部通过）

- 单元测试 **45/45** ✅
- 计算器数学题：「加→五→等于」→ 显示屏 **10** ✅，光标零移动
- 插件端到端：`ui.click --name 七` → 自动走 `uia-invoke` → 显示屏 **7** ✅，耗时 **2.9 秒**
- 计算器已关闭，无残留窗口

### 发布状态

- **v0.1.27 已发布**：[GitHub Release](https://github.com/helloo-666/dsh-screen-helper/releases/tag/v0.1.27)
- 装机副本已同步（`C:\Users\Hello\.dsh\profiles\desktop\node_modules\dsh-screen-helper`，版本 0.1.27）
- 重启 DSH 会话后 profile 配置生效

### 期间的失败与返工（如实记录）

- 给 `dsbox.ps1` 插入 `ui invoke` 代码时，用"查找替换行号"的方式连续 3 次插错位置，造成 if/else 结构断裂（报 `elseif` 不是命令的错误），文件里一度出现 3 个重复的 `ui` 分支
- 最终做法：删光所有残缺分支，整体重写一个干净的 `ui` 分支（find / inspect / invoke / tree 四个子命令），一次通过
- **教训**：超过 20 行的结构改动不该用打补丁方式，应该直接重写整个函数/分支

---

## 2026-09-25 中午（第 24 轮工作）

### 完成：v0.1.26 已发布

**主题：点击后验证闭环 + 中文乱码修复**

| 改动 | 效果 |
|------|------|
| dsbox 新增 `ui inspect --point` | 点击后能反查"这个点落在了什么控件上"，验证从"屏幕变了没"升级为"落点身份对不对" |
| `ui find` 优先返回 ClickablePoint | 修了窗口标题中心点落在最小化按钮上的定位偏差 |
| UTF-8 输出强制 | 修了中文经 cmd.exe 传递变乱码的问题（`设置`变 4 个替换符） |

### 实测记录（计算器完整闭环）

```
启动计算器 → probe 预检 → 找到「五」按钮 → 后台点击
→ OCR 显示屏 = 0（后台消息被 UWP 忽略，如实报告）
→ 切 real 模式点击 → 显示屏 = 5 ✅
→ 用 UIA 读屏幕确认，不靠截图
→ 关闭计算器，不留垃圾
```

### 一个小事故（已妥善处理）

- 测试时误启动了记事本，触发了 Windows 的"会话恢复"，弹出一堆空便签窗口
- 处理：逐个用 UIA 确认内容为空后才关闭；你两份有内容的文件（《ChatGPT 便笺》《HELLO-BG》）**原样保留，一个字没动**

---

## 历史版本一览

| 版本 | 主题 | 状态 |
|------|------|------|
| 0.1.23 | dsbox 诞生：WinRT OCR 替代 SAH（16s→1s），窗口框提示 | ✅ 已发布 |
| 0.1.24 | dsbox 补齐鼠标滚动/键盘输入，插件自动路由 | ✅ 已发布 |
| 0.1.25 | UIA 树 + probe 预检 + ui.click 走 UIA 精确坐标（6.1s→2.7s） | ✅ 已发布 |
| 0.1.26 | 点位反查验证 + ClickablePoint + 中文乱码修复 | ✅ 已发布 |
| 0.1.27 | UIA InvokePattern（"AI 自己的鼠标"，零光标点击） | ✅ 已发布 |
